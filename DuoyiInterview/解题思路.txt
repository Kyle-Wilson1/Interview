1.直接调用相关函数转换  O(1)
2.用一个指针记录数字的开始位置，扫描一遍字符串，当遇到非数字就计算前面出现的数字串长度，更新已经存在的数字串长度，并将指针移到该位置，继续扫描   O(n)
3.扫描一遍数组，用两个变量记录出现的数字与该数字出现的次数，初始用第一个数字，然后每扫描一个数字就比较与之前的数字是否一样，相同就次数加1，不同则减1，如果次数为0则更新为其它的数字；因为一个数字如果比一半还多，则相互进行抵消，最终次数肯定大于1   O(n)

MaxOs系统下编译运行
